Django>=4.2,<5.0
django-crispy-forms>=2.0
pillow>=9.0.0  # For image uploads
gunicorn>=20.1.0  # Optional: For deployment
psycopg2>=2.9.0  # Only if using PostgreSQL, otherwise remove
whitenoise>=6.0  # For serving static files in production
dj-database-url>=1.0.0  # For managing database URL
django-environ>=0.9.0  # For environment variables
